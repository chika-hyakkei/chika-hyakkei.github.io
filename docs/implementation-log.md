# 実装履歴

## 2026-07-22 80点ロードマップの確定

### 目的

新機能の数ではなく、安定性、初見の分かりやすさ、攻略差、ビルド、バランス、演出品質を順番に改善し、地下百景を80点相当の完成度へ引き上げる。

### 実装内容

`docs/roadmap-80.md` を追加し、評価配点、第0〜第8段階、各段階の合格条件、後回しにする機能を記録した。100階踏破60〜90分、難しいが公平、モンスターは発見式、アイテム詳細は入手後解放という方針をADR-006として確定した。

### 主要な処理の流れ

今後は第0段階の基準計測から始め、各段階でコード、テスト、実機確認、資料更新を完了してから次へ進む。

### 設計上の注意点

80点到達前は、無限階層、ガチャ、オンライン対戦、大量の職業追加など、評価軸を広げる機能を優先しない。

### 影響範囲

開発の優先順位と完了判定。今回、ゲーム本体、セーブ形式、ランキングAPIは変更しない。

## 2026-07-21 アイテム・モンスターリスト

### 目的

探索中でもタイトル画面でも、収集対象と攻略情報を確認できる入口を用意する。

### 実装内容

既存のモンスター図鑑を「モンスター」ボタンから開けるようにし、100種類のアイテムを武器・防具・装飾品・消耗品・遺物に分けたアイテムリストを追加した。

### 影響範囲

タイトル・探索画面下部の操作、図鑑モーダル、静的レンダリングテスト。セーブデータには影響しない。

## 2026-07-21 終了処理の復旧と操作案内の明確化

### 目的

ランキングの自動記録導入後も「冒険を諦める」を確実に完了させ、スマートフォンの操作案内を誤読しにくくする。

### 実装内容

終了処理で使うランキング状態のsetterを復旧し、中断・死亡などの終了処理が例外で止まらないようにした。フッターの移動案内を「タップ・クリック」と明記した。

### 影響範囲

冒険の終了、ランキング自動送信、画面下の操作案内。セーブ・ランキングDBの形式は変更しない。

## 2026-07-21 Cloudflare Web Analyticsの導入

### 目的

ゲームサイトへの訪問数、流入元、スマートフォン/PC比率を把握し、宣伝施策の効果を比較できるようにする。

### 実装内容

Cloudflareダッシュボードに `chika-hyakkei.github.io` のWeb Analyticsサイトを作成した。`app/layout.tsx` でCloudflare提供のbeaconを読み込み、フッターに匿名アクセス集計の利用を表示した。設計、送信範囲、制約は `docs/architecture.md`、`docs/data-and-save.md`、ADR-004へ記録した。

### 主要な処理の流れ

ページを開くとCloudflareのbeaconが匿名のページ訪問情報を集計する。集計結果はCloudflareダッシュボードの **Analytics → Web analytics → chika-hyakkei.github.io** で確認する。ゲームのセーブ、探索行動、名前、テスト記録はこの計測へ渡さない。

### 設計上の注意点

beaconトークンは公開ページに含まれる識別子であり管理権限を持たない。一方、Cloudflareの管理APIトークンやログイン情報はリポジトリに保存しない。追跡防止機能による未計測があり得るため、数字は施策の比較に使い、厳密な実人数とみなさない。

### 影響範囲

初回ページ表示時の外部通信、タイトル・ゲーム画面下部の案内、公開ビルド、開発資料。通常セーブ、テスト記録、ランキングデータ形式は変更しない。

### 未対応・今後の課題

「開始」「10階到達」などゲーム内の到達率まで測る場合は、別途、送信項目を限定したイベント計測を設計する。

## 2026-07-21 ヘッダータイトルのロゴ化

### 目的

タイトル画面の左上を、文字組みではなく地下百景専用の看板ロゴとして見せる。洞窟内の冒険イラストとは役割を分け、イラストの上へ重ねない。

### 実装内容

`public/assets/title-logo.webp` を追加し、`app/page.tsx` のヘッダーにあった英字キッカーと `h1` を `site-logo` の画像へ置き換えた。`app/globals.css` にはPC・スマートフォンそれぞれで記録表示と共存できる表示幅を追加した。元画像の背景は、外周につながる色だけを透明化して宝石装飾の色を残している。公開用は透過対応のWebPにして容量も抑える。

`app/updates.ts` にも今回の更新を追加し、タイトル画面の更新要約と履歴画面から確認できるようにした。

### 主要な処理の流れ

タイトル画面描画時に、ヘッダー左側が `/assets/title-logo.webp` を表示する。右側の最高記録、投票ボタン、音声ボタンは既存のまま表示する。タイトル下の洞窟イラストは独立しており、ロゴは重ねない。

### 設計上の注意点

ロゴの正本は公開用の `public/assets/title-logo.webp` とする。制作途中の画像は `promo/` に置き、追跡対象にしない。ヘッダー幅は狭い画面で操作ボタンを押し出さないようCSSで縮小する。

### 影響範囲

タイトル画面の見た目、公開アセット、更新履歴、静的レンダリングテスト。ゲーム進行とセーブデータ形式には影響しない。

### 未対応・今後の課題

実機でタイトルヘッダーの幅を確認し、必要ならロゴ表示幅だけを微調整する。

## 2026-07-20 タイトルの更新要約・週間TOP3と商店の満杯表示

### 目的

公開後の更新内容と今週の上位記録を、ゲーム開始前に邪魔にならない範囲で見せる。あわせて、持ち物が満杯の商店購入を誤解なく扱う。

### 実装内容

`app/updates.ts` を更新履歴のデータ元として追加した。タイトルの左下に最新更新と週間TOP3を表示し、更新履歴は別モーダルで全件を確認できるようにした。週間TOP3は既存ランキングAPIから読取専用で取得する。

商店購入も確認した。既存コードは、交換対象の装備があり持ち物8個なら金貨を減らす前に購入を中断していた。今回、該当商品のボタンを無効化して「持ち物がいっぱい」と表示し、UI上でも購入不可を明示した。

### 主要な処理の流れ

タイトル表示時に週間ランキングを最大3件だけ取得する。更新内容は静的な `UPDATE_NOTES` から最新1件を表示し、履歴ボタンで全件を表示する。ランキング取得に失敗した場合はTOP3を空表示にし、ゲーム開始を妨げない。

### 設計上の注意点

週間TOP3の読取は自動だが、プレイ結果の送信は任意のまま維持する。今後の更新時は `app/updates.ts` の先頭へ1件追加し、必要に応じてこの履歴も更新する。

### 影響範囲

タイトル画面、ランキングAPIの読取、商店UI、開発資料。セーブデータ形式は変更しない。

### 未対応・今後の課題

ランキングが空の場合の文言は表示するが、通信エラーの詳細はタイトル画面へ出さない。

## 2026-07-19 長期開発資料の整備

### 目的

会話の圧縮やセッション切替後も、地下百景の現状・設計意図・未対応事項を安全に引き継ぐ。

### 実装内容

`AGENTS.md` と `docs/` に、現在地、全体構成、ゲーム仕様、保存・ランキング仕様、規則、ADR、課題を追加した。READMEからも資料の入口を案内する。

### 主要な処理の流れ

次回の作業者は `AGENTS.md` → `README.md` → `docs/now.md` → 変更範囲の資料 → 関連コードの順で確認する。

### 設計上の注意点

資料はコードに代わる仕様ではない。通常は実行中コードを事実として扱い、保存・外部送信の不一致だけは実装前に確認する。

### 影響範囲

開発・保守手順のみ。ゲームの実行ロジック、セーブデータ、公開挙動は変更しない。

### 未対応・今後の課題

資料の更新を機能変更と同じコミットに保つ運用を継続する。

## 2026-07-19 オンラインランキング基盤

### 目的

プレイヤーが任意で到達記録を共有できるようにする。

### 実装内容

ゲームに地下ランキング画面と結果送信UIを追加し、`ranking-api/` にCloudflare Worker/D1のAPIと移行SQLを追加した。ゲームはGitHub Pagesのまま、ランキングだけ外部APIに分離した。

### 主要な処理の流れ

結果画面で公開名を入力して送信すると、匿名端末ID単位の自己ベストがWorkerへ送られる。WorkerはD1の総合・週間ベストを更新し、ランキング画面は上位100件を取得する。

### 設計上の注意点

テストモードと途中放棄は送信しない。送信は任意で、自動送信しない。クライアント値を信頼するカジュアルランキングである。

### 影響範囲

`app/page.tsx`、`app/ranking.ts`、`public/ranking-config.js`、`ranking-api/`、Cloudflare D1/Worker、GitHub Pages。

### 未対応・今後の課題

不正対策を強化するなら、サーバー発行シードと行動ログ検証を設計する。

## 2026-07-21 終了記録の自動ランキングと今回行の強調

### 目的

死亡・中断を含む通常プレイの結果を必ずランキングへ残し、終了直後に自分の記録を見つけられるようにする。

### 実装内容

ランキングAPIに冒険ごとの `ranking_runs` テーブルを追加し、旧自己ベストを初回移行するSQLを作成した。通常プレイの終了時は自動送信し、送信後に週間ランキングを開く。APIが返す記録IDをクエリへ渡し、該当行だけを黄色の点滅で強調する。

### 設計上の注意点

テストモードは送信しない。ランキングは自己ベストではなく終了した冒険ごとの履歴であり、同じプレイヤーの複数行が表示される。

### 影響範囲

ランキング画面、Cloudflare Worker/D1、公開時のD1移行、ランキング仕様資料。

## 2026-07-22 第0〜第1段階・復旧可能なセーブとランキング再送

### 目的

80点ロードマップの比較基準を固定し、黒画面、セーブ破損、ランキング通信断で冒険や終了記録を失う経路を減らす。

### 実装内容

`docs/qa-baseline.md` に状態遷移、既知リスク、3環境の確認表を追加した。`app/storage.ts` にRunとMetaの構造検査、直前正常値バックアップ、破損値隔離、復元を追加した。`app/page.tsx` に復旧通知と描画エラー境界を設けた。`app/ranking.ts` は終了記録を送信前に端末へ保留し、起動時に再送する。Worker/D1には冒険ごとの `submissionId` と一意制約を追加した。

### 主要な処理の流れ

Run保存時は現在の正常値をバックアップしてから新しい値を書く。起動時に現在値が壊れていれば原文を隔離し、直前値を復元する。終了時は先にゲーム内処理を完了し、終了記録を端末キューへ入れてから送る。通信失敗時は保留したまま次回起動で再送し、Workerは同じIDなら既存行を返す。

### 設計上の注意点

冒険が正常終了した場合はRunとバックアップを両方削除する。隔離値は自動削除しない。Workerは公開切替中の旧クライアントから `submissionId` なしの投稿も一時的に受理し、サーバー側でIDを補う。

### 影響範囲

RunのlocalStorage、ランキング送信、Cloudflare Worker/D1、エラー時UI、GitHub Pages公開、保存・ランキング資料。

### 未対応・今後の課題

iPhone Safari、Android Chrome、PC Chromeの実機確認、主要終了処理100回、戦闘中・商店中・階移動直後の画面操作を伴う復元試験は未確認。第1段階の残作業として継続する。

## 2026-07-22 第1段階の自動回帰完了と総合ランキング一本化

### 目的

終了状態と代表セーブの回帰を繰り返し検証し、参加人数が少ない現状でランキング記録を分散させない。

### 実装内容

終了後のRun解放・結果保持・ランキング対象判定を `app/run-end.ts` の純粋関数へ分離した。死亡・中断・帰還・踏破を均等に含む100回テストを追加した。戦闘中、商店中、階移動直後の保存復元テストを追加した。ランキング画面、タイトルTOP3、Worker取得APIを総合順位へ統一した。

### 主要な処理の流れ

通常プレイ終了時は結果を保持してRunを解放し、総合ランキングを開いて記録を自動送信する。タイトル表示時も総合上位3件を取得する。Workerは `scope` の有無に関係なく全終了記録を得点順で返す。

### 設計上の注意点

既存D1行と `week_key` は変更・削除しない。過去版クライアントが `scope=weekly` を送っても総合順位を返し、公開切替中の互換性を維持する。

### 影響範囲

終了処理、タイトル画面、ランキング画面、ランキングWorker、回帰テスト、QA資料。

### 未対応・今後の課題

ブラウザで1280×900、iPhone相当390×844、Android相当412×915のタイトル表示と横はみ出しを確認し、Android相当幅では通常開始後の地図・ステータス・十字キーも確認した。PC Chrome、iPhone Safari、Android Chromeの実機固有操作と実機からのセーブ復元は未確認のため継続する。

## 2026-07-22 HP・MPの10分割宝石ゲージ

### 目的

ステータス画面と戦闘画面で横幅を取っていた単色バーを小さくし、地下百景のレトロRPG画面に合う情報表示へ統一する。

### 実装内容

`app/page.tsx` に共通の `GemGauge` を追加し、ステータスと戦闘HUDのHP・MP表示を置き換えた。各ゲージは数値、属性色の小宝石、10個の区画を持つ。`app/grim.css` では黒鉄と金縁、HPの赤宝石、MPの青宝石、端数の部分点灯、低HP時の明滅を実装した。

### 主要な処理の流れ

現在値を最大値で割り、10区画それぞれの点灯割合へ変換する。HPとMPは横並びにし、ステータス画面ではPC幅126px・スマートフォン幅171px、戦闘画面ではPC幅204px・スマートフォン幅129pxで表示する。

### 設計上の注意点

見た目だけで残量を判断させず、現在値と最大値の数値、`role="meter"` と読み上げ名を維持する。HP/MPのゲーム計算や保存形式は変更しない。

### 影響範囲

ステータス画面、戦闘画面、低HP演出、レスポンシブ表示、画面更新履歴。

### 未対応・今後の課題

実機iPhone Safari・Android Chromeでの最終的な文字サイズ確認は継続する。

## 2026-07-22 第2段階・初見10分の案内

### 目的

初めて遊ぶ人が説明書を読まずに、移動、敵予告、防御、回復、状態異常の判断を最初の戦闘から理解できるようにする。

### 実装内容

`app/onboarding.ts` に案内進捗の補完と状況別の案内判定を追加し、`app/page.tsx` に初回だけ出る非モーダル小窓を実装した。端末のポインター種別によりタップ用とキーボード用の操作文を切り替える。状態異常は残りターン、効果、回復薬での解除をステータス欄と戦闘HUDへ表示する。技のMP不足、回復薬・爆裂石の未所持、階層主からの逃走不能はコマンド内に理由を表示する。暗闇と麻痺の未実装だった戦闘効果も表示内容に合わせて追加した。

### 主要な処理の流れ

通常プレイ開始後、未確認の移動案内を出す。確認後に戦闘へ入ると敵予告、強攻撃の予告時に防御、状態異常付与時に効果と解除、HP65%以下かつ薬所持時に回復を順次案内する。各確認状態はRunやMetaと別のlocalStorageへ保存する。案内終了では全項目を確認済みにし、フッターの再表示操作では安全に初期化する。

### 設計上の注意点

案内は操作を止める全画面モーダルにしない。テストモードでは表示しない。状態異常名だけでなく残りターン・実際の効果・解除方法を一致させる。利用不能コマンドを押してもターン演出や攻撃エフェクトを開始せず、理由メッセージだけを出す。

### 影響範囲

初回プレイ、探索・戦闘画面、状態異常処理、戦闘ショートカット、商店の回復薬表示、localStorage、タイトル更新履歴、回帰テスト。

### 未対応・今後の課題

iPhone SafariとAndroid Chrome実機で小窓の文字サイズを確認する。実プレイで案内の出る間隔と、案内後に強攻撃へ防御を選べる割合を観察する。第2.5段階で表示文言を翻訳キーへ移す。

## 2026-07-22 第3段階前半・1〜20階の敵攻略差

### 目的

魔物を名前・色・数値だけで区別せず、敵の兆候と次行動を見て攻撃、防御、技を変える戦闘へ移行する。100体一括変更による難易度急変を避け、序盤20階で完成形を検証する。

### 実装内容

`app/monsters.ts` を100体の名前一覧から戦闘マスターへ拡張し、弱点、耐性、固有能力、兆候、攻略法、行動パターン、HP・攻撃補正を持たせた。1〜20階では10素体を出現させ、分裂、二連噛み、吸血、金貨盗み、甲殻防御、呪い反撃、炎吸収、瀕死暴走、固定暗闇を実装した。10・20階の階層主へ固定行動とHP半分後の別パターンを追加した。図鑑は遭遇、1、3、5体討伐で情報を段階開示する。

### 主要な処理の流れ

階層と敵種から既存の図鑑IDを選び、戦闘開始時に遭遇済みをMetaへ記録する。攻撃種別を物理、炎、氷、雷、聖、打撃へ分類し、弱点なら1.35倍、耐性なら0.55倍に補正する。敵の身構え中は攻撃を軽減し、固有能力に応じて分裂回復、吸血回復、金貨減少、呪い付与などを処理する。次行動はマスターの配列を順に読み、瀕死または階層主HP段階で別配列へ切り替える。

### 設計上の注意点

敵の固有能力は `app/monsters.ts` を正とし、画面だけに条件を重複定義しない。階層主へ通常敵の固有能力を誤適用しない。ダメージ表示は弱点・耐性・防御補正後の実減少値と一致させる。状態異常予告への防御は持続を1ターンへ短縮する。苔スライムの分裂済みだけは戦闘保存に必要なため `traitUsed` を任意項目として保持する。

### 影響範囲

1〜20階の敵生成、通常戦闘、10・20階階層主、攻撃種別、状態異常防御、戦闘保存、魔物図鑑、スマートフォン戦闘画面、固定シード試験、タイトル更新履歴。

### 未対応・今後の課題

21階以降の15素体と30階以降の8階層主は従来挙動を基礎にしており、固有能力の実戦接続は未対応。階層主10体を図鑑100体へ含める再編は、既存討伐IDの移行設計後に行う。iPhone Safari・Android Chrome実機と、説明なしの外部プレイヤーによる攻略理解は未確認。

## 2026-07-23 第3段階後半・全階層の敵攻略差

### 目的

1〜20階で検証した敵の兆候、固有能力、弱点、耐性、固定行動を21〜100階へ広げ、敵名を見て行動を変える戦闘を全編で成立させる。

### 実装内容

`app/monsters.ts` の残り15素体へ暗闇泥弾、毒糸、盾壁、強化火柱、MP吸収、麻痺、出血、守備貫通、三連撃、出血反撃、麻痺針、魔法反射、再生、呪い、瀕死狩りを追加した。4変異は行動配列、弱点、耐性、状態異常を変える。30〜100階を含む全10階層主へ前半・HP半分以下の後半パターン、固有能力、弱点、耐性、攻略表示を追加した。

21階以降の敵抽選は図鑑添字を100加算して保存し、4変異を一意に識別する。100未満の旧番号は従来式で読み、旧戦闘セーブの不足 `catalogId` も補完する。図鑑100ID、Run v3、Meta v2は変更していない。

### 主要な処理の流れ

階層帯から候補素体と変異範囲を決め、図鑑IDへ直結する敵番号を生成する。戦闘開始時はマスターから兆候・行動配列・能力補正を取得する。敵ターンでは守備貫通、追加倍率、MP吸収、麻痺針、再生などを処理し、プレイヤー攻撃時は盾壁、出血反撃、炎吸収、魔法反射、変異別弱点・耐性を処理する。階層主はHP半分を境に `phasePattern` へ切り替える。

### 設計上の注意点

強い固有能力を足した15素体はHP・攻撃補正を下げ、能力込み脅威値を0.80〜1.00、平均0.93未満に保つ。魔法反射は物理・爆裂石という代替手段を必ず残す。状態異常予告は防御で1ターンへ短縮できる。旧 `kind` と新 `kind` の読み分けを削除しない。

### 影響範囲

21〜100階の敵生成、25素体・4変異、通常戦闘、全階層主、図鑑グラフィック変異、旧戦闘セーブ補完、タイトル更新履歴、固定脅威試験。

### 未対応・今後の課題

390×844のブラウザ表示、30階の変異敵戦闘、60階の開始表示、横はみ出しなしを確認した。iPhone Safari・Android Chrome実機、全職業での魔法反射・MP吸収・再生の長期到達率、説明なしの外部プレイヤーによる攻略理解は未確認。階層主10体を図鑑100枠へ含める再編は既存討伐数の移行設計まで保留する。

## 2026-07-24 第2.5段階・多言語化の土台

### 目的

日本語版の挙動を維持したまま、後から英語版を安全に完成できる翻訳・言語切替・データ識別の基盤を作る。

### 実装内容

`app/i18n.ts` に日本語辞書、部分英語辞書、端末言語判定、手動設定、日本語フォールバックを追加した。タイトル、職業、地下依頼、主要戦闘コマンド、図鑑入口を翻訳キーへ接続し、ヘッダーへ日本語・英語切替を追加した。地下依頼は表示文ではなく `slay`、`descend`、`chests`、`test` のIDをRunへ保存する。

### 主要な処理の流れ

初回起動時は保存済み言語を優先し、未設定なら端末言語が英語系の場合だけ英語、それ以外は日本語を選ぶ。手動切替は `chika-hyakkei-locale-v1` へ保存し、HTMLの `lang` も更新する。翻訳キーが英語辞書にない場合は必ず日本語辞書を表示する。旧Runの日本語地下依頼は読込時に対応IDへ変換する。

### 設計上の注意点

職業、地下依頼、アイテム、魔物、ランキングの識別には表示文を使わない。翻訳漏れを空文字や例外にせず日本語へ戻す。Run v3・Meta v2・ランキング形式は変更しない。

### 影響範囲

タイトル画面、ヘッダー、職業表示、地下依頼、戦闘コマンド、図鑑入口、Run読込、端末内設定、スマートフォンヘッダー。

### 未対応・今後の課題

探索・商店・図鑑詳細・ランキング・初回案内・戦闘ログはまだ日本語中心であり、第6.5段階で全面英訳する。英語端末の実機自動判定とiPhone Safari・Android Chromeでの長文折返しは未確認。

## 2026-07-25 第4段階前半・装備ビルド中核

### 目的

100アイテム一覧と実際の装備能力の二重管理を解消し、単純な攻撃・守備の更新ではなく、同じ職業で戦い方を選べる装備ビルドを成立させる。

### 実装内容

`app/items.ts` に装備80件の能力値と、吸血、会心、防御反撃、MP循環、状態耐性、瀕死強化の6効果を持たせた。`app/page.tsx` の装備生成、商店、宝箱、装備表示、戦闘計算はこの共通マスターを参照する。商店は階層ごとに装備系統を巡回し、現在装備との差と特殊効果を購入前に表示する。持ち物と装備欄にも効果・比較を表示する。

Meta v2へ任意の `itemDiscovery` を追加し、所持、装備、遺物、回復薬、爆裂石から入手済みIDを補完する。アイテム一覧は全件の存在と分類を表示し、未入手時は効果を伏せ、入手後だけ詳細を開示する。タイトル更新履歴と自動テストも更新した。

### 主要な処理の流れ

アイテムIDから能力と特殊効果を含む装備を生成する。攻撃命中時は会心・瀕死強化・吸血・MP循環を順に処理し、敵の状態異常時は状態耐性、防御後の被弾時は防御反撃を処理する。発動結果は戦闘ログへ明記する。商店では候補と現在装備の攻撃・守備・HP・MP差、および特殊効果変更を表示する。

### 設計上の注意点

アイテム名や表示文から効果を判定しない。装備内の `passive` と `passiveValue` は任意項目とし、旧Run・墓は既存IDから不足効果だけを補完して元の能力と個体情報を保持する。同種効果は現在加算する。Meta v2の番号は維持し、旧Metaの不足 `itemDiscovery` は空で補完する。ロールバック時は旧版が追加項目を無視できる。

### 影響範囲

100アイテムマスター、装備80件、宝箱、商店、装備・持ち物表示、通常戦闘、状態異常、遺物説明、Meta保存、アイテム一覧、タイトル更新履歴。

### 未対応・今後の課題

万能薬、火炎瓶、氷結札、雷鳴玉、煙玉、解呪札、止血布、星蜜の8件は一覧マスターに正式効果を定義したが、宝箱・所持・戦闘使用への接続を残す。6特殊効果の全職業・長期到達率とスマートフォン実機表示は未確認。`npm test` 37件と `npm run build:pages` を実行して確認する。

## 2026-07-25 第4段階後半・消耗品の実戦接続

### 目的

一覧にだけ存在した8消耗品を実際の冒険へ接続し、装備80件、消耗品10件、遺物10件の100アイテムすべてを入手・利用できる状態にする。

### 実装内容

`app/page.tsx` のRunへID別個数を持つ任意の `supplies` を追加し、宝箱、入手済み記録、探索ステータス、破棄操作、戦闘用道具袋へ接続した。万能薬、火炎瓶、氷結札、雷鳴玉、煙玉、解呪札、止血布、星蜜の効果を実装し、戦闘中は `5` または `I` でも道具袋を開閉できる。`app/grim.css` にスマートフォンでも残数と使用不可理由を読める道具袋表示を追加し、`app/updates.ts` と設計資料を更新した。

### 主要な処理の流れ

宝箱抽選で追加消耗品を得たら、道具袋の合計が5個未満ならID別個数を増やし、Metaの入手済み記録へ反映する。満杯なら14Gへ変換する。戦闘使用時は解除対象、MP残量、階層主戦を先に検査し、無効なら消費しない。属性攻撃は敵の弱点・耐性、防御、炎吸収、魔法反射を共通戦闘規則で処理し、撃破時は通常の勝利処理へ合流する。煙玉以外の成功使用は敵の予告済み手番へ進む。

### 設計上の注意点

回復薬 `potions` と爆裂石 `bombs` の既存形式・上限は変更しない。追加消耗品だけを安定IDの `supplies` へ保存し、名前で効果を判定しない。旧Runは空の道具袋を補完し、Run v3とMeta v2の番号を維持する。効果がない状態の操作は手番・個数を消費しない。

### 影響範囲

宝箱の入手抽選、Run保存・復元、Metaのアイテム発見、探索ステータス、戦闘コマンド、属性相性、状態異常、逃走、キーボード操作、タイトル更新履歴。

### 未対応・今後の課題

固定シード自動試験とビルドは通過したが、追加消耗品を含む1万シードの長期到達率、全職業差、道具袋上限5個、宝箱出現率、390px幅を含むiPhone Safari・Android Chrome実機操作は次の第5段階で確認する。

## 2026-07-25 逃走直後の再戦防止

### 目的

敵との戦闘から逃げ切っても、次の移動時に同じ敵が追跡して即座に再戦となる不具合を直し、逃走を有効な戦術にする。

### 実装内容

`app/escape.ts` に逃走対象だけを同じ階の離れた空き通路へ移す純粋関数を追加した。`app/page.tsx` の通常逃走成功と煙玉の両方から共通利用し、タイトル更新履歴とゲーム仕様を更新した。`tests/escape.test.mjs` で対象敵だけが移動すること、敵数・他の敵・空き床がない場合を壊さないことを検証する。

### 主要な処理の流れ

逃走成功時に戦闘中の敵IDを受け取り、プレイヤー、他の敵、特殊マスと重ならない通常床を列挙する。その中でプレイヤーから最も離れた床へ対象敵だけを移し、探索へ戻る。敵は削除せず、討伐数・経験値・金貨も増やさない。

### 設計上の注意点

逃走失敗、階層主戦、勝利処理、他の敵の位置は変更しない。敵の再配置は乱数を消費せず、Runの既存 `enemies` 座標としてそのまま自動保存するため、セーブ形式の変更はない。

### 影響範囲

通常逃走、盗賊の逃走補正、煙玉、探索時の敵追跡、Run自動保存、タイトル更新履歴。

### 未対応・今後の課題

自動テストでは最遠床への移動と非破壊性を確認した。狭い実マップでの見え方、逃走後に意図的に同じ敵を追った場合の再遭遇感覚は実機プレイで継続確認する。

## 2026-07-25 第5段階・固定シード難易度計測

### 目的

初見相当と敵予告を活用するプレイの到達率を、全職業・複数装備方針で再現可能に比較し、感覚だけの難易度変更を防ぐ。

### 実装内容

`scripts/balance-simulator.mjs` に6職業、攻撃・防御・継戦の3ビルド、初見・予告活用の2戦術を扱う固定シードモデルを追加した。宝箱の抽選帯、3階ごとの商店、階移動時18%回復とMP3、回復薬・爆裂石・道具袋上限、職業差をモデルへ反映する。`scripts/balance-report.mjs` は合計36,000走を実行し、10・30・50・70・90・100階の到達率、残HP/MP、残薬・道具、死因、所要時間、職業・ビルド別結果を出力する。`tests/balance.test.mjs` は最低1万走、目標帯、50階到達、決定性、資源上限を自動検査する。

### 主要な処理の流れ

各職業・ビルド・戦術へ同じ規則の固定シードを与え、100階まで階移動回復、宝箱、商店購入、代表戦闘消耗、危険行動への対処を順番に処理する。チェックポイント到達時に資源と時間を加算し、死亡時は一撃、消耗、状態異常、MP枯渇へ分類する。36,000走の集計では初見3.84%、予告活用13.13%、推定77.9〜84.7分となった。

### 設計上の注意点

シミュレーターは回帰用の抽象モデルであり、迷路経路、画面操作、全敵の実際のターン列を再生する完全AIではない。目標帯へ収まったため、今回は敵攻撃力、回復薬45%、宝箱抽選、商店価格を変更していない。モデル値だけを実プレイヤーの到達率として扱わない。

### 影響範囲

開発用バランス試験、CI時間、難易度基準、QA資料、ロードマップ、タイトル更新履歴。ゲーム中の能力値、セーブ形式、ランキング形式には変更なし。

### 未対応・今後の課題

匿名テスト記録・ランキングとモデル値の比較、5人以上の説明なし実プレイ、iPhone・Androidでの実所要時間は未確認。装備特殊効果の発動順や敵ごとの全行動を再現する高精度AIは、実測との大きな差が見つかった場合に範囲を絞って追加する。新規シミュレーター3ファイルの限定ESLintは通過したが、リポジトリ全体の `npm run lint` は既存の同期script指摘と生成済み `site/` の解析を含むため失敗した。

## 2026-07-25 第6段階前半・戦闘ポーズと着弾同期

### 目的

入力方法や技名に左右されず、攻撃、防御、魔法、治療、道具の姿を正しく描き分け、HP表示が演出より先に変化する違和感をなくす。

### 実装内容

`app/battle-presentation.ts` に戦闘コマンドID・職業からポーズと画面効果を返す共通表を追加した。`app/page.tsx` から日本語ラベル監視とメッセージ内容による重複エフェクト判定を外し、クリック・キーボードを `battleAction` の同一経路へ統合した。表示専用の味方・敵HPを追加し、内部HP変化から味方220ms、敵250ms後にゲージへ反映する。`app/grim.css` には治療・回復薬の瓶、爆裂石の投擲、逃走の離脱ポーズを既存衣装のまま追加した。

### 主要な処理の流れ

コマンド入力時に使用可否を確認し、使用可能なら安定IDからポーズと効果を開始して戦闘処理を実行する。敵HPの内部値は攻撃計算時に確定し、表示だけ250ms保持して剣・魔法の着弾で更新する。敵手番は従来どおり650ms後に処理し、味方HP表示をさらに220ms保持して敵斬撃中に更新する。

### 設計上の注意点

表示用HPは演出専用で、死亡、勝利、状態異常、セーブには使わない。防御、戦士の鉄壁、僧侶の守り、騎士の盾技は盾ポーズ、僧侶の治療は回復ポーズ、魔法使いの攻撃魔法と賢者の大治療・雷撃は魔法ポーズとする。戦闘文や翻訳文から行動種別を判定しない。

### 影響範囲

戦闘ボタン、キーボードショートカット、主人公ポーズ、画面効果、HP宝石ゲージ、敵HPバー、低HP疲労表示、戦闘中の入力制御。能力値、難易度、セーブ形式には変更なし。

### 未対応・今後の課題

46件の自動テストとビルドで対応表・遅延範囲・主要回帰を確認した。iPhone Safari・Android Chromeでの実際の一拍、勝利時の消滅演出、10階帯の背景・装飾・BGM差、全BGMと効果音の音量密度統一は第6段階後半で確認する。

### 追記: 賢者の詠唱ポーズ

ユーザー確認により、賢者は回復役として道具を使う姿ではなく、万能職として大治療も雷撃も魔法を詠唱する方針へ修正した。大治療は魔法ポーズに青い回復効果、雷撃は魔法ポーズに攻撃魔法効果を組み合わせる。能力値、MP消費、HP反映時刻は変更しない。

## 2026-07-25 第6段階後半・地下十景と地域BGM

### 目的

4種類の洞窟色が深層で繰り返す状態を改め、10階進むたびに未知の場所へ来た手応えを、地図、戦闘、音のすべてで伝える。深層の音を単純に大きくせず、長時間遊んでも聴き疲れしにくい密度へ揃える。

### 実装内容

`app/depth-themes.ts` に1〜100階を10帯へ分ける地域マスターを追加し、苔むす入口から奈落王城までの地域名、短い情景文、記号装飾、BGM変奏を定義した。`app/page.tsx` は階番号から地域を導出し、階表示、地図装飾、戦闘表示、地域境界の進入文、BGMへ接続する。`app/grim.css` は岩・床の模様、光、境界線、戦闘背景を10地域分実装した。

`app/music.ts` は探索、通常戦闘、階層主、商店の既存主旋律とテンポを維持しつつ、地域別に移調、主音色、対旋律、低音パルス、発音密度を変更する。密度2〜4では出力係数を段階的に下げ、深層の追加レイヤーがそのまま音量増加にならないよう調整した。瞬間的に突出していた爆裂石などの効果音は共通上限へ収めた。タイトルの更新要約と地域境界テストも追加した。

### 主要な処理の流れ

現在階を10で区切って地域番号1〜10へ丸め、同じ地域オブジェクトを探索画面、戦闘画面、BGM開始処理へ渡す。10→11階など新地域の初階だけ地域名と情景文を表示する。場面切替または地域切替でBGMを再開し、場面曲へ地域固有の半音移調・音色・対旋律を重ねる。

### 設計上の注意点

地域番号はRunへ保存せず、必ず階番号から導出する。これによりRun v3、Meta v2、ランキング形式を変更せず、旧セーブをそのまま表示できる。BGM変奏は既存の場面別主旋律とテンポを置き換えない。視覚テーマもゲーム能力、敵抽選、宝箱、難易度へ影響させない。

### 影響範囲

1〜100階の探索地図、階表示、地域境界メッセージ、通常戦闘、階層主戦、商店BGM、戦闘背景、タイトル更新履歴、自動テスト。セーブデータ、戦闘数値、ランキングAPIには影響しない。

### 未対応・今後の課題

自動テストと静的公開ビルドは通過した。iPhone Safari、Android Chromeの本体スピーカーとイヤホンで、10地域の暗部、文字可読性、探索・通常戦闘・階層主・商店の体感音量を実機確認する。外部プレイヤーが地域名や景観を区別して覚えられるかは第8段階で確認する。

## 2026-07-25 第6.5段階・全面英語表示と帰還誤操作防止

### 目的

海外向けの直接導線を用意し、日本語の既存セーブを壊さず主要画面と攻略情報を英語で遊べる状態にする。同時に、階層主撃破後の頻度が低く取り消せない帰還を誤って確定しにくくする。

### 実装内容

`app/content-localization.ts` を追加し、100アイテム、100体の魔物、10体の階層主、10地域、装備特殊効果を安定IDから英語表示へ変換した。`app/page.tsx` の開始、探索、戦闘、商店、装備、道具袋、図鑑、ランキング、初回案内、終了画面を日英表示へ接続した。`app/en/page.tsx` と静的出力の `/en/index.html` を追加した。

階層主後は潜行継続を先頭の主ボタンにし、帰還ボタンを押すと確認欄を開く。冒険終了とランキング登録を説明し、もう一度「帰還を確定する」を押した場合だけ既存の終了処理へ進む。英語の長いコマンドと確認欄向けのスマートフォンCSS、自動テスト、タイトル更新要約も追加した。

### 主要な処理の流れ

起動時に `/en/` なら英語、それ以外は端末保存言語とブラウザ言語から初期値を決める。ゲーム状態は従来のRun・Metaを読み、描画時にアイテムID、魔物ID、階番号から表示文を選ぶ。英語画面では日本語で保存された自由形式ログを露出させず、現在の探索・戦闘・商店状態を英語で要約する。帰還副ボタンは画面内の確認状態だけを変更し、最終確定時だけ `finish("return")` を呼ぶ。

### 設計上の注意点

翻訳文はセーブ・ランキング・戦闘条件・演出条件に使わない。日本語マスターと安定IDは維持し、表示だけを差し替える。帰還確認状態は一時的なUI状態でありRunへ保存しない。潜行継続には確認を増やさない。

### 影響範囲

タイトル、探索、戦闘、商店、装備、道具、図鑑、ランキング、初回案内、結果画面、GitHub Pages静的出力、ボス撃破後の選択画面。Run v3、Meta v2、ランキングAPI、戦闘数値、難易度には変更なし。

### 未対応・今後の課題

51件の自動テスト、静的公開ビルド、ローカルブラウザで `/en/` のタイトルから探索開始までを確認した。新規翻訳・公開ファイルの限定ESLintは通過した。リポジトリ全体のESLintは、既存の `page.tsx` にある描画中状態更新や `useSupply` の命名をHookと誤認する指摘を含むため未通過。iPhone Safari・Android Chromeで英語の折返し、英語ネイティブによる語感、10〜90階の帰還確認操作は実機未確認。英語戦闘ログは現在フェーズの安全な要約であり、攻撃結果を一文ずつ英訳する詳細イベントログは今後の利用状況を見て判断する。

## 2026-07-26 宝箱ドット絵と戦闘背景の整理

### 目的

探索中の宝箱を記号一文字ではなく、報酬として見つけたくなる立体的な見た目へ改める。戦闘背景の中央縦線を取り除き、洞窟の奥行きを邪魔しない画面にする。

### 実装内容

`app/page.tsx` の宝箱を3枚の木板を持つ専用マークアップへ変更し、`app/grim.css` で金属の外枠、丸い蓋、木板、中央の錠前、鍵穴、落ち影を描画した。地域色は既存の発光だけに使用する。通常戦闘と10地域テーマ双方の `battle.modal:before` から、中央50%に置いていた縦のグラデーションを削除した。

### 設計上の注意点

宝箱の見た目は表示専用であり、地図タイル、開封判定、抽選、持ち物上限、セーブには変更を加えない。中央線だけを除去し、左右の小さな洞窟光と既存の地域色は維持する。

### 影響範囲

探索地図上の未開封宝箱、通常戦闘、10地域の戦闘背景、タイトル更新履歴、自動テスト。戦闘数値、Run v3、Meta v2、ランキングAPIには影響しない。

### 未対応・今後の課題

PCとスマートフォン実機で、床の暗い地域でも宝箱の鍵穴・外枠が読めるかを確認する。
